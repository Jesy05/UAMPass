@{
    ViewData["Title"] = "Recuperar Contraseña";

    // Detecta si la vista fue llamada desde AdminAuth o desde EstudianteAuth
    bool esAdmin = ViewBag.EsAdmin as bool? ?? false;
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <div class="card shadow-sm border-0">
                <div class="card-body p-4">

                    <h3 class="text-center mb-3">Recuperar Contraseña</h3>
                    <p class="text-muted text-center">
                        Ingresa tu correo para recuperar el acceso a tu cuenta.
                    </p>

                    @if (ViewBag.Mensaje != null)
                    {
                        <div class="alert alert-info text-center">
                            @ViewBag.Mensaje
                        </div>
                    }

                    <form asp-action="ForgotPassword" method="post">
                        <div class="form-group mb-3">
                            <label>Correo electrónico</label>
                            <input type="email"
                                   name="correo"
                                   class="form-control"
                                   placeholder="tu@correo.com"
                                   required />
                        </div>

                        <button type="submit" class="btn w-100 text-white" style="background-color: #04a7ad;">
                            Enviar instrucciones
                        </button>
                    </form>

                    <div class="text-center mt-3">
                        <a asp-controller="@(esAdmin ? "AdminAuth" : "EstudianteAuth")"
                           asp-action="Login"
                           style="color: #027d81;">
                            ← Regresar al inicio de sesión
                        </a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>
